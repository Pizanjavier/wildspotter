/**
 * Legacy type aliases — kept for backward compatibility with saved spots
 * in AsyncStorage. New code should use types from @/services/api/types.
 */
export type { SpotSummary, SpotDetail, LegalStatus, LegalCheck } from '@/services/api/types';
